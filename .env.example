# KiotRetail local development environment template.
# Copy to .env (gitignored) and fill in real values.
# DO NOT commit .env. The DatabaseUtil reads these as fallback when JNDI is not configured.
DB_URL=jdbc:sqlserver://localhost:1433;databaseName=DBFinora;trustServerCertificate=true
DB_USER=sa
DB_PASSWORD=changeme
